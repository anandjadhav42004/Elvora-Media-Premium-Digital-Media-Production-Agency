# Workspace Rules & Preferences

- **Automatic GitHub Push**: Always stage (`git add .`), commit with a descriptive message, and push (`git push origin main`) all completed feature updates, fixes, and changes to the GitHub repository automatically upon task completion.
